olist_duckdb:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Path is relative to where you run `dbt` from (the dbt/ folder).
      # If running from project root instead, change to: analytics.duckdb
      path: "../analytics.duckdb"
      threads: 4

